apiVersion: v1
kind: Service
metadata:
  name: obs
  namespace: monitoring
  labels:
    app.kubernetes.io/name: obs
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: obs
  ports:
    - name: metrics
      port: 2112
      targetPort: metrics
      protocol: TCP
